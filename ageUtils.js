
/**
 * Comprueba si una persona es mayor de edad.
 * @param {number} edad - La edad de la persona.
 * @returns {boolean} - True si es mayor de edad, false de lo contrario.
 */
function isAdult(age) {
    return age >= 18;
}

module.exports = { isAdult };
